// Shared constants for the quiz

import type { Circle } from "./types";

export const ANCHORS = ["Not yet", "Sometimes", "Mostly", "Consistently"] as const;
export const CIRCLES: Circle[] = ["ESSENTIALS", "EXPLORING", "SUPPORTING", "LEADING"];
